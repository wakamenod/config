:PROPERTIES:
:ID:       526034f9-d614-4dc6-b829-96f865c39481
:END:
#+title: Org-Roam
#+category: Configuration
#+filetags: Configuration

* OrgRoam
#+begin_src elisp
;; -*- lexical-binding: t; -*-
;;; see https://systemcrafters.net/build-a-second-brain-in-emacs/5-org-roam-hacks/

;; (require 'dash)

;; (defun my/org-roam-project-finalize-hook ()
;;   "Adds the captured project file to `org-agenda-files' if the
;; capture was not aborted."
;;   ;; Remove the hook since it was added temporarily
;;   (remove-hook 'org-capture-after-finalize-hook #'my/org-roam-project-finalize-hook)

;;   ;; Add project file to the agenda list if the capture was confirmed
;;   (unless org-note-abort
;;     (with-current-buffer (org-capture-get :buffer)
;;       (add-to-list 'org-agenda-files (buffer-file-name)))))

;; ;; Bind this to C-c n I
;; (defun org-roam-node-insert-immediate (arg &rest args)
;;   (interactive "P")
;;   (let ((args (cons arg args))
;;         (org-roam-capture-templates (list (append (car org-roam-capture-templates)
;;                                                   '(:immediate-finish t)))))
;;     (apply #'org-roam-node-insert args)))

;; (defun my/org-roam-filter-by-tag-and-todo (tag-names todo-name)
;;   (lambda (node)
;;     (and (if tag-names (--every-p (member it (org-roam-node-tags node)) tag-names) t)
;;          (if todo-name (string-equal todo-name (org-roam-node-todo node)) t))))

;; (defun my/org-roam-refresh-agenda-list ()
;;   (interactive)
;;   (setq org-agenda-files (append (my/org-roam-list-nodes-by-tag '("Task")) (list my/org-agenda-calendar))))

;; (defun my/org-roam-list-nodes-by-tag (tag-names)
;;   (mapcar #'org-roam-node-file
;;           (seq-filter
;;            (my/org-roam-filter-by-tag-and-todo tag-names nil)
;;            (org-roam-node-list))))

;; (defun my/org-roam-list-tags (tag-name)
;;   (delq nil (mapcar (lambda (x) (if (string-equal tag-name x) nil x))
;;                     (delete-dups (apply #'append
;;                                         (mapcar #'org-roam-node-tags
;;                                                 (seq-filter
;;                                                  (my/org-roam-filter-by-tag-and-todo `(,tag-name) nil)
;;                                                  (org-roam-node-list))))))))
;; (defun my/org-roam-select-tag(tag-name)
;;   (completing-read "Tag:" (my/org-roam-list-tags tag-name)))

;; (defun my/org-roam-find-task ()
;;   (interactive)
;;   ;; Add the todo file to the agenda after capture is finished
;;   (add-hook 'org-capture-after-finalize-hook #'my/org-roam-project-finalize-hook)

;;   ;; Select a todo file to open, creating it if necessary
;;   (org-roam-node-find
;;    nil
;;    nil
;;    (my/org-roam-filter-by-tag-and-todo nil "TODO")))
;; ;; todo: template 使うか検討
;; ;; :templates
;; ;; '(("p" "project" plain "* Goals\n\n%?\n\n* Tasks\n\n** TODO Add initial tasks\n\n* Dates\n\n"
;; ;;    :if-new (file+head "%<%Y%m%d%H%M%S>-${slug}.org" "#+title: ${title}\n#+category: ${title}\n#+filetags: Project")
;; ;;    :unnarrowed t))))

;; (defun my/org-roam-find-reference ()
;;   (interactive)
;;   (add-hook 'org-capture-after-finalize-hook #'my/org-roam-project-finalize-hook)
;;   (org-roam-node-find
;;    nil
;;    nil
;;    (my/org-roam-filter-by-tag-and-todo '("Reference") nil)))

;; (defun my/org-roam-find-job-note ()
;;   (interactive)
;;   (add-hook 'org-capture-after-finalize-hook #'my/org-roam-project-finalize-hook)
;;   (org-roam-node-find
;;    nil
;;    nil
;;    (my/org-roam-filter-by-tag-and-todo '("JobNote") nil)))

;; (defun my/org-roam-find-configuration ()
;;   (interactive)
;;   (add-hook 'org-capture-after-finalize-hook #'my/org-roam-project-finalize-hook)
;;   (org-roam-node-find
;;    nil
;;    nil
;;    (my/org-roam-filter-by-tag-and-todo '("Configuration") nil)))

;; (defun my/org-roam-find-blog-post ()
;;   (interactive)
;;   (add-hook 'org-capture-after-finalize-hook #'my/org-roam-project-finalize-hook)
;;   (org-roam-node-find
;;    nil
;;    nil
;;    (my/org-roam-filter-by-tag-and-todo '("Blog") nil)))

;; (defun my/org-roam-capture-task ()
;;   (interactive)
;;   ;; Add the project file to the agenda after capture is finished
;;   (add-hook 'org-capture-after-finalize-hook #'my/org-roam-project-finalize-hook)
;;   (let* ((tag (my/org-roam-select-tag "Task"))
;;          (header (format "\n#+category: %s\n#+filetags: %s Task" tag tag))
;;          (node (org-roam-node-read
;;                 nil
;;                 (my/org-roam-filter-by-tag-and-todo `(,tag "Task") "TODO")))
;;          (is-new-node (not (org-roam-node-file node)))
;;          (olp (concat "TODO " (org-roam-node-title node) header))
;;          (dir (concat org-roam-directory "task/" tag)))

;;     (unless (file-exists-p dir)
;;       (make-directory dir))

;;     (if is-new-node
;;         (org-roam-capture- :node node
;;                            :templates `(("p" "project" plain "** TODO %?"
;;                                          :if-new (file+olp ,(concat dir "/%<%Y%m%d%H%M%S>-${slug}.org")
;;                                                            (,olp)))))
;;       (org-roam-capture- :node node
;;                          :templates `(("p" "project" plain "** TODO %?"
;;                                        :if-new (node ,(org-roam-node-id node))))))))

;; (defun my/org-roam-capture-reference ()
;;   (interactive)
;;   ;; Add the project file to the agenda after capture is finished
;;   (add-hook 'org-capture-after-finalize-hook #'my/org-roam-project-finalize-hook)
;;   (let* ((tag (my/org-roam-select-tag "Reference"))
;;          (header (format "\n#+title: ${title}\n#+category: %s\n#+filetags: %s Reference" tag tag))
;;          (node (org-roam-node-read
;;                 nil
;;                 (my/org-roam-filter-by-tag-and-todo `(,tag "Reference") nil)))
;;          (dir (concat org-roam-directory "reference/" tag)))

;;     (unless (file-exists-p dir)
;;       (make-directory dir))

;;     (org-roam-capture- :node node
;;                        :templates `(("p" "project" plain "* %?"
;;                                      :if-new (file+head ,(concat dir "/%<%Y%m%d%H%M%S>-${slug}.org")
;;                                                         ,header
;;                                                         ))))))

;; (defun my/org-roam-capture-blog-post ()
;;   (interactive)
;;   ;; Add the blog post file to the agenda after capture is finished
;;   (add-hook 'org-capture-after-finalize-hook #'my/org-roam-project-finalize-hook)
;;   (let* ((tag (my/org-roam-select-tag "Blog"))
;;          (header (format "\n#+title: ${title}\n#+category: %s\n#+filetags: %s Blog" tag tag))
;;          (node (org-roam-node-read
;;                 nil
;;                 (my/org-roam-filter-by-tag-and-todo `(,tag "Blog") nil)))
;;          (dir (concat org-roam-directory "blog")))

;;     (unless (file-exists-p dir)
;;       (make-directory dir))

;;     (org-roam-capture- :node node
;;                        :templates `(("p" "project" plain "* %?"
;;                                      :if-new (file+head ,(concat dir "/%<%Y%m%d%H%M%S>-${slug}.org")
;;                                                         ,header
;;                                                         ))))))

;; (defun my/org-roam-capture-job-note ()
;;   (interactive)
;;   (add-hook 'org-capture-after-finalize-hook #'my/org-roam-project-finalize-hook)
;;   (let* ((tag (my/org-roam-select-tag "JobNote"))
;;          (header (format "\n#+title: ${title}\n#+category: %s\n#+filetags: %s JobNote" tag tag))
;;          (node (org-roam-node-read
;;                 nil
;;                 (my/org-roam-filter-by-tag-and-todo `(,tag "JobNote") nil)))
;;          (dir (concat org-roam-directory "job/" tag)))

;;     (unless (file-exists-p dir)
;;       (make-directory dir))

;;     (org-roam-capture- :node node
;;                        :templates `(("p" "project" plain "* %?"
;;                                      :if-new (file+head ,(concat dir "/%<%Y%m%d%H%M%S>-${slug}.org")
;;                                                         ,header
;;                                                         ))))))

;; (provide 'my-org-roam)
;;; my-org-roam.el ends here
#+end_src
